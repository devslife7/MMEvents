<h1>Location Show Page</h1>

<div>City: <%= @location.city %></div><br>
<div> Hotels: </div>
<ul>
    <% @location.hotels.each do |hotel| %>
        <li><%= link_to hotel.name, hotel_path(hotel.id) %></li>
    <% end %>

</ul>